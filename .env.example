# Copy to .env.local if you need overrides. Never commit secrets.
# Ollama and MCP do not belong here either — they live off-repo.

IX_OSC_HOST=127.0.0.1
IX_OSC_PORT=9000
IX_RUNTIME_HTTP_PORT=9100
IX_OLLAMA_URL=http://127.0.0.1:11434
