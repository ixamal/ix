# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
ENV/

# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env & secrets — never commit
.env
.env.*
!.env.example

# vercel / typescript
.vercel
*.tsbuildinfo
next-env.d.ts

# Local MCP & hardware bridges (defense in depth; live copies are off-repo)
.cursor/mcp.json
.cursor/mcp.local.json
local_adapters/
mcp_scripts/private/
*.soc
*.ipc

# Agent scratch
agent-tools/

# Crate reports stay off-repo (~/local_tools/crate/reports)
crate/reports/

# Live DJ collections — absolute /Users/<name>/ paths, never public git
databases/*.nml
databases/*.nml.*
databases/*.xml
databases/*.db
databases/*.bak

